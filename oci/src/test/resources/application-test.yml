datasources:
  default:
    driverClassName: oracle.jdbc.OracleDriver
    db-type: oracle
    dialect: ORACLE
micronaut:
  metrics:
    export:
      oraclecloud:
        enabled: false
        namespace: test
flyway:
  datasources:
    default:
      enabled: true