# Secrets via Oracle Cloud Vault
micronaut:
  config-client:
    enabled: true
oci:
  config:
    instance-principal:
      enabled: true
  vault:
    config:
      enabled: true
    vaults:
      - compartment-ocid: ${COMPARTMENT_ID}
        ocid: ${VAULT_OCID}
