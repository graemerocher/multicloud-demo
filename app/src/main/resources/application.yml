micronaut:
  server:
    thread-selection: IO
  router:
    static-resources:
      images:
        enabled: true
        paths: classpath:images
        mapping: /images/**
      css:
        enabled: true
        paths: classpath:css
        mapping: /css/**
netty:
  default:
    allocator:
      max-order: 3
